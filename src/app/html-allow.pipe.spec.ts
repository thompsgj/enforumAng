import { HtmlAllowPipe } from './html-allow.pipe';

describe('HtmlAllowPipe', () => {
  it('create an instance', () => {
    const pipe = new HtmlAllowPipe();
    expect(pipe).toBeTruthy();
  });
});
